# PC Inventory Builder

Monorepo con frontend React + Vite y backend Express + Prisma.

## Scripts principales

- `npm install`
- `npm run dev:server`
- `npm run dev:client`
- `npm run prisma:migrate`
- `npm run prisma:seed`
