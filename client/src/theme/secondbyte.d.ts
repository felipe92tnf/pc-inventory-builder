/** Tipos para `secondbyte.js` (paleta SecondByte). */
export const sb: {
  pageWrap: string;
  bgApp: string;
  header: string;
  navActive: string;
  navInactive: string;
  pageHeader: string;
  summaryCard: string;
  filterPanel: string;
  listCard: string;
  sectionBlock: string;
};
