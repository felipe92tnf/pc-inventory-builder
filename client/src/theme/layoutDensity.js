/**
 * Ritmo vertical compacto (reutilizar en páginas y cáscaras de sección).
 * Solo clases Tailwind; sin colores nuevos.
 */
/** Stack entre bloques principales (max-w-7xl). */
export const PAGE_OUTER_7XL = "mx-auto w-full max-w-7xl space-y-4 px-2 pb-6 text-slate-100 md:space-y-5 md:px-4";
/** Variante inventario (max-w-6xl). */
export const PAGE_OUTER_6XL = "mx-auto w-full max-w-6xl space-y-4 px-2 pb-6 text-slate-100 md:space-y-5 md:px-4";
/** Ventas: un poco más de aire entre bloques KPI + listas. */
export const PAGE_OUTER_7XL_SALES = "mx-auto w-full max-w-7xl space-y-5 px-2 pb-6 text-slate-100 md:space-y-6 md:px-4";
/** Cáscara de bloque / tabla / formulario en tarjeta. */
export const SECTION_SHELL = "rounded-2xl border border-slate-800 bg-slate-900/80 p-4 shadow-lg shadow-slate-950/40 md:p-5";
/** Cabecera de página con gradiente (título + acciones). */
export const PAGE_HERO = "rounded-2xl border border-slate-800 bg-gradient-to-r from-slate-950 via-slate-900 to-indigo-950 p-5 shadow-[0_20px_50px_-24px_rgba(79,70,229,0.75)]";
/** Celdas de tabla densas (th/td). */
export const TABLE_CELL = "px-3 py-2";
/** Cabecera de `<details>` (listado colapsable). */
export const DETAILS_SUMMARY_ROW = "flex cursor-pointer list-none items-center justify-between gap-3 px-4 py-3";
